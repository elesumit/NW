// const { runTestForPage } = require('../../support/testUtils.js');
// const config = require('../../../testConfig.json');

// Cypress.on('uncaught:exception', (err, runnable) => {
//   return false;
// });

// // Run the test for the 'businessInsurance' page
// const pageName = 'businessInsurance';
// const pageUrl = config.environments.pages[pageName];
// if (pageUrl) {
//   runTestForPage(pageName, pageUrl);
// } else {
//   console.log(`Page ${pageName} not found in the configuration.`);
// }
